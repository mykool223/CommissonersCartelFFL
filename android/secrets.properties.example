# Copy to secrets.properties and fill in. Gitignored, like Config/Secrets.xcconfig
# on iOS. The anon key is safe to ship in a build — row level security is what
# protects the data — but it does not belong in the repo.
SUPABASE_HOST=yourproject.supabase.co
SUPABASE_ANON_KEY=eyJ...
ESPN_LEAGUE_ID=847540747
